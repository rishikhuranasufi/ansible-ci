version: 2.1

jobs:
  ansible-lint:
    working_directory: /tmp/ansible
    steps:
    - checkout

  run-ansible:
    working_directory: /tmp/ansible
    steps:
    - checkout
   
workflows:
  ansible-using-orbs:
    jobs:
      - ansible-lint
      - run-ansible:
          requires:
            - ansible-lint
